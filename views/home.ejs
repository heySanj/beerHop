<% layout('layouts/boilerplate') %> 

<h1>Hop to your local Brewery! 🍻</h1>
<ul class="list-group">
  <% for( let bar of breweries ) { %>
    <a href="/breweries/<%= bar._id %>" class="list-group-item list-group-item-action"><%= bar.name %> - <%= bar.location %></a>
  <% } %>
</ul>